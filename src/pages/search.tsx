import { Box, Typography } from "@mui/material"

export const Search = () => {
  return (
    <Box sx={{ flexGrow: "1", backgroundColor: "lightblue" }}>
      {" "}
      <Typography variant="h3">welcome from search component</Typography>
    </Box>
  )
}
